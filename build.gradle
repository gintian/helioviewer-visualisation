subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile 'junit:junit:4.12'
    }

    tasks.withType(JavaExec) {
        jvmArgs = ['-Xms4096m', '-Xmx4096m']
    }
}